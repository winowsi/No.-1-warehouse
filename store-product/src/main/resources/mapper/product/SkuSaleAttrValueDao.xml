<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.winowsi.product.dao.SkuSaleAttrValueDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.winowsi.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
    <resultMap id="SkuItemSaleAttrVo" type="com.winowsi.product.vo.SkuItemSaleAttrVo">
        <result property="attrId" column="attrId"></result>
        <result property="attrName" column="attrName"></result>
        <collection property="attrValue" ofType="com.winowsi.product.vo.AttrValueWithSkuIdVo">
            <result property="attrValue" column="attrValue"></result>
            <result property="skuIds" column="skuIds"></result>
        </collection>

    </resultMap>
    <select id="getSaleAttrBySpuId" resultMap="SkuItemSaleAttrVo">
        SELECT pssav.attr_id attrId,
               pssav.attr_name attrName,
               pssav.attr_value attrValue,
               GROUP_CONCAT(DISTINCT psi.sku_id) skuIds
        from pms_sku_info psi
                 LEFT JOIN pms_sku_sale_attr_value pssav on psi.sku_id = pssav.sku_id
        where psi.spu_id = #{spuId}
        group by pssav.attr_id, pssav.attr_name, pssav.attr_value
    </select>
    <select id="getSkuSaleAttrValue" resultType="java.lang.String">
        SELECT concat(attr_name,':',attr_value)from pms_sku_sale_attr_value pssav
        where sku_id=#{skuId}
    </select>


</mapper>